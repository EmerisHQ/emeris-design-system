export enum Size {
  md = 'md',
  sm = 'sm'
}

export enum Align {
  left = 'left',
  center = 'center',
  right = 'right'
}

export enum ButtonVariant {
  primary = 'primary',
  secondary = 'secondary'
}

export enum ButtonStatus {
  loading = 'loading',
  active = 'active'
}
